# Optional — LLM extraction (OpenAI-compatible API)
# Without these, /ai/extract uses rules-only (recommended for local dev).

LEAPTO_AI_API_KEY=
LEAPTO_AI_BASE_URL=https://api.openai.com/v1
LEAPTO_AI_MODEL=gpt-4o-mini
